# ── 직접 의존성 ───────────────────────────────────────────────
# Python 3.12 기준으로 검증됨

# LLM 호출 (main_agent.py — OpenAI 호환 엔드포인트)
openai==2.26.0

# YAML 워크플로우 로드 (investment-simulation.yaml)
pyyaml==6.0.2

# 환경 변수
python-dotenv==1.2.2

# AI 오케스트레이션 (user_functions.py — @kernel_function 데코레이터)
semantic-kernel==1.40.0

# 시뮬레이션 결과 시각화 (graph/graph.ipynb)
matplotlib==3.10.1
numpy==2.2.3

# ── 핵심 전이 의존성 (버전 고정) ────────────────────────────
aiohttp==3.13.3
