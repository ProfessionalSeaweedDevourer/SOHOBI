# 2026-04-05 작업 요약

## 개요

| 항목 | 내용 |
|------|------|
| 날짜 | 2026-04-05 |
| 작업 브랜치 | `PARK` |
| 커밋 수 | 26개 (약 14:58 ~ 00:19, 약 9시간) |
| 관련 PR | #148, #149, #150 |

---

## 기능 추가 (Feature)

### 1. 인라인 피드백 위젯 (계층 2) — `862681d`

답변 카드 하단에 👍/👎 버튼과 부정 태그 선택 UI 추가.

- `frontend/src/components/InlineFeedback.jsx` — 상태 머신 (idle → selected → submitted)
- `frontend/src/components/FeedbackTags.jsx` — 태그 선택 UI
- `integrated_PARK/feedback_router.py` — 피드백 Cosmos DB 저장 라우터
- `frontend/src/components/ResponseCard.jsx` — InlineFeedback 통합

### 2. 사용 이벤트 추적 (계층 3-A) — `c612e3e`

사용자 행동 이벤트를 Cosmos DB에 기록하는 추적 시스템 구현.

- `integrated_PARK/event_router.py` — POST /api/event 라우터
- `frontend/src/utils/trackEvent.js` — 공통 추적 유틸
- 6개 페이지에 이벤트 삽입: `agent_query`, `response_view`, `feature_discovery`, `checklist_view`, `roadmap_view`, `report_view`

### 3. 창업 준비 체크리스트 (계층 3-B) — `b377c10`

8개 항목의 창업 준비 단계 체크리스트 구현.

- `integrated_PARK/checklist_router.py` — GET/PATCH /api/checklist/{session_id}
- `frontend/src/components/checklist/ChecklistItem.jsx`
- `frontend/src/components/checklist/ChecklistProgress.jsx`
- `frontend/src/pages/UserChat.jsx` — 체크리스트 패널 통합

### 4. 사용 리포트 시스템 (계층 3-C) — `3db0d1a`

세션별 에이전트 사용·피드백·체크리스트 진행 현황 대시보드.

- `integrated_PARK/report_router.py` — GET /api/report/{session_id} (피드백·이벤트·체크리스트 집계)
- `frontend/src/pages/MyReport.jsx`
- `frontend/src/components/report/ReportSummary.jsx` — 대시보드 카드 3개
- `frontend/src/components/report/AgentUsageChart.jsx` — 에이전트 사용 차트
- `frontend/src/components/report/Recommendations.jsx` — 맞춤 추천 바로가기

### 5. 로드맵 투표 위젯 (계층 3-D) — `9945fa3`

향후 기능 로드맵 공개 및 사용자 투표 기능.

- `integrated_PARK/roadmap_router.py` — 투표 라우터 (POST /api/roadmap/vote)
- `frontend/src/pages/Roadmap.jsx` — 투표 UI
- 관리자 뷰: 투표 집계 LogViewer 탭 추가 (`34f53fd`)

### 6. Google OAuth 로그인 + 대화 내역 조회 — `b6c90c2`

Google 계정 기반 인증 및 Q&A 기록 조회 기능.

- `integrated_PARK/auth_router.py` — OAuth 콜백, /api/auth/my
- `frontend/src/context/AuthContext.jsx`
- `frontend/src/pages/AuthCallback.jsx`
- `frontend/src/pages/MyLogs.jsx` — 대화 내역 목록

### 7. report API 누락 필드 보강 + 스펙 완성 — `4f1d018`, `b1ccfce`

- `report_router.py`: `top_negative_tags`, `last_active`, `first_active`, `most_used_agent` 필드 추가
- `ReportSummary.jsx`: 첫 사용일·최근 사용일 날짜 표시
- `MyReport.jsx`, `Recommendations.jsx`: `report_view` / `report_recommendation_click` 이벤트 추가

---

## 보안 강화 (Security)

### devAuth HMAC-SHA256 토큰 교체 — `b4d830f`

개발자 모드에서 콘솔 우회 가능하던 세션 저장 방식을 HMAC-SHA256 서명 토큰으로 대체.

- `integrated_PARK/auth.py`

### SWA 프록시 URL 은폐 시도 → 롤백 — `2cff746`, `9cb781b`

Azure Static Web Apps Free 티어에서 외부 URL 리라이트 미지원으로 인해 프록시 방식을 폐기하고 `VITE_API_URL` 직접 호출 방식으로 복원.

### 미인증 엔드포인트 API Key 인증 적용 — `55d8b07`

5개 라우터(map, realestate, feedback, event, roadmap)에 `verify_api_key` 의존성 추가. `X-API-Key` 헤더로 전환.

- `integrated_PARK/auth.py` — `verify_api_key` 함수
- 경로 탐색(path traversal) 취약점 수정: `/map/load-csv` `realpath` 검증 추가

### IDOR·PII·입력값 검증·API 인증 수정 — `aaa2eaf`

| 취약점 | 수정 내용 |
|--------|----------|
| IDOR | checklist/report GET·PATCH에 `session_exists()` 소유권 검증 추가 |
| PII | `conversationContext` 백엔드 null 처리 |
| 입력값 검증 | Pydantic `Field(max_length=...)` + `Literal` 타입 제약 |
| API 인증 | `API_SECRET_KEY` 빈 문자열 시 인증 무효화 방지 |

---

## 버그 수정 (Fix)

| 커밋 | 내용 |
|------|------|
| `630bb84` (#150) | `staticwebapp.config.json`을 `public/`으로 이동 — Vite 빌드 후 SWA 404 해결 |
| `a5c24db` | Cosmos SDK 4.15.0 호환성 — `query_items`의 `enable_cross_partition_query` 파라미터 제거 |
| `b659458` (#148) | 유저명 클릭 시 로그아웃 드롭다운 분리 + 로드맵 투표 `voter_id` 검증 추가 |

---

## CI/CD 개선

| 커밋 | 내용 |
|------|------|
| `02f36b5` | `.github/workflows/cleanup-staging.yml` — SWA 스테이징 환경 자동 정리 워크플로우 |
| `aaa2eaf` | 배포 파이프라인에 `API_SECRET_KEY` 환경변수 설정 추가 |

---

## 기타

- `7b59e59`: `docs/plans/`·`docs/session-reports/` 파일명에서 연도(2026) 제거 (53개 파일 리네임)
- `521b207`: CLAUDE.md에 PR 생성 후 테스트 실행 루틴 추가
- `a076b69`: LogViewer에 사용자 피드백 탭 추가 + Blob 60초 TTL 캐시 적용

---

## 관련 PR

| PR | 제목 | 상태 |
|----|------|------|
| [#148](https://github.com/ProfessionalSeaweedDevourer/SOHOBI/pull/148) | 유저명 클릭 시 드롭다운으로 로그아웃 분리 | MERGED |
| [#149](https://github.com/ProfessionalSeaweedDevourer/SOHOBI/pull/149) | report API 누락 필드 보강 | MERGED |
| [#150](https://github.com/ProfessionalSeaweedDevourer/SOHOBI/pull/150) | staticwebapp.config.json public/으로 이동 | MERGED |
