# 2026-03-26 작업 요약

## 개요

| 항목 | 내용 |
|------|------|
| 날짜 | 2026-03-26 |
| 작업 브랜치 | `PARK` |
| 커밋 수 | 약 10건 |
| 관련 PR | #49, #50 |

---

## 기능 추가 (Feature)

### 1. 상권 에이전트 DB Oracle 외부 서버 전환 — `2967b46` (#49)

SQLite 로컬 파일 → Oracle 외부 서버 기반 상권 DB로 전환.

- `integrated_PARK/plugins/location_plugin.py` — Oracle 연결 DAO 구현
- `.env.example` — `ORACLE_HOST`, `ORACLE_PORT`, `ORACLE_SID` 등 환경변수 템플릿 추가 (`bc0eef0`)
- Oracle 연결을 DSN 문자열 대신 host/port/sid 개별 파라미터로 변경 (`d17c83a`)

### 2. 재무 시뮬레이션 차트 프론트엔드 통합 출력 — `85f2ef3` (#50)

백엔드에서 base64 PNG로 생성된 차트를 프론트엔드 응답 카드에 인라인 표시.

- `frontend/src/components/ResponseCard.jsx` — chart 필드 있을 시 `<img>` 렌더링
- `frontend/src/pages/UserChat.jsx` — `current_params` state 누적 전달 로직

### 3. 프론트엔드 API URL Railway → Azure Container Apps 전환

- `.github/workflows/azure-static-web-apps-*.yml` `VITE_API_URL` 변경
- Railway 백엔드 완전 종료

---

## 버그 수정 (Fix)

| 커밋 | 내용 |
|------|------|
| `8a1daaa` | 배포 환경 matplotlib 한글 폰트 깨짐 수정 — 번들 폰트(`nam/malgun.ttf`) 직접 등록 |
| `2d0e1bd` | 프론트엔드 API URL을 Railway에서 Azure Container App으로 전환 |
| `d17c83a` | Oracle 연결 DSN 문자열 → host/port/sid 파라미터로 변경 |

---

## 보안 (Security)

### Signoff 프롬프트 인젝션 1차 대응 — `78c5fcc`

draft 삽입 전 `<<<DRAFT_START>>>` / `<<<DRAFT_END>>>` 구분자로 감싸 signoff 평가 범위를 명확히 제한.

- `signoff/signoff_agent.py` — 구분자 삽입 로직 추가
- `prompts/signoff_*/evaluate/skprompt.txt` (4개) — 구분자 안내 문구 추가
- `api_server.py` — 의심 패턴 감지 후 `sohobi.security` 로거 경고 기록 (차단 아님)

---

## 기타

- 이날 Railway → Azure 이전 완료로 단일 계정(`ejp-9638`) 기반 아키텍처 확립
- 기존 Railway 로그 76건 + Azure Blob 3건 머지하여 79건 통합 (`scripts/merge_logs.py`)
- 도메인별 테스트 질문 25개 작성 (세션 리포트에 기록)

---

## 관련 PR

| PR | 제목 | 상태 |
|----|------|------|
| [#49](https://github.com/ProfessionalSeaweedDevourer/SOHOBI/pull/49) | 상권 에이전트 DB Oracle 외부 서버 전환 | MERGED |
| [#50](https://github.com/ProfessionalSeaweedDevourer/SOHOBI/pull/50) | 재무 시뮬레이션 차트 프론트 통합 출력 | MERGED |
